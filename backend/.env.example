DATABASE_URL="postgresql://postgres:<PASSWORD>@<HOST>:5432/postgres?schema=public"
DIRECT_URL="postgresql://postgres:<PASSWORD>@<HOST>:5432/postgres?schema=public"
JWT_SECRET="dev-secret-change-me"
JWT_REFRESH_SECRET="dev-refresh-secret-change-me"
TEST_USERS_PASSWORD="AchAquiTest123"
BACKEND_URL="http://localhost:3000"

# ── Supabase Storage (obrigatório para upload de fotos) ───────────────────────
# Obtém em: dashboard.supabase.com → Project Settings → API
# SUPABASE_URL    = https://<project-ref>.supabase.co
# SUPABASE_SERVICE_ROLE_KEY = chave "service_role" (NÃO a anon key)
# SUPABASE_STORAGE_BUCKET   = nome do bucket criado (ex: achaqui-public)
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""
SUPABASE_STORAGE_BUCKET="achaqui-public"